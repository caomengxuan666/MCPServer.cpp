@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Include the targets file
include("${CMAKE_CURRENT_LIST_DIR}/MCPServer++Targets.cmake")

# Add definitions for the user
set(MCP_INCLUDE_DIRS "${PACKAGE_PREFIX_DIR}/include")
set(MCP_LIBRARIES mcp_core mcp_protocol mcp_transport mcp_business mcp_plugin_sdk)

check_required_components(MCPServer++)